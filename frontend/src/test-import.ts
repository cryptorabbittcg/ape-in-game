// Test file to check if GameMode can be imported
import { GameMode, GameModeValues } from './types/game'

console.log('GameMode imported successfully:', GameMode)
console.log('GameModeValues imported successfully:', GameModeValues)

export const testGameMode: GameMode = 'sandy'
